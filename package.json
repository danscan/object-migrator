{
  "name": "object-migrator",
  "version": "0.0.3",
  "description": "Function factory for running async iterators on object property values.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dan Scanlon",
  "license": "ISC",
  "dependencies": {
    "q": "^1.0.1"
  }
}
